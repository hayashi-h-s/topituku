
<div class="jumbotron text-center">
  <h1 class="display-4">ランとぴ。にログイン</h1>
  <%= form_for(resource, as: resource_name,class: "inline-block",url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email",class: "form-label-group form" %>
    </div>

    <div class="field">
      <%= f.label :password %><br />
      <%= f.password_field :password, autocomplete: "current-password",class: "form-label-group form" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>
    <%= f.submit "登録",class: "btn btn-primary submit-btn form" %>

  <% end %>
</div>

  <%= form_for(resource, as: resource_name,class: "inline-block",url: session_path(resource_name)) do |f| %>
    <%= f.hidden_field :email, value: 'max9173627@gmail.com' %>
    <%= f.hidden_field :password, value: 'hayahaya' %>
    <%= f.submit 'テストログイン', class: 'btn btn-default' %>
  <% end %>

